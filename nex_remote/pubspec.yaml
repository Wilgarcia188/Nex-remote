name: nex_remote
description: "Nex Remote - Android TV keycode debugging tool (Phase 1)."
publish_to: 'none'

version: 2.0.0+2

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
